const copy = {
  fr: {
    navTechnology: "Technologie",
    navProducts: "Intégration",
    navApplications: "March\u00e9s",
    navProcess: "Prototype",
    navContact: "Contact",
    navCta: "Cadrer un projet",
    heroEyebrow: "Technologie brevet\u00e9e pour textile et \u00e9quipement",
    heroTitle: "La fermeture magn\u00e9tique con\u00e7ue pour dispara\u00eetre.",
    heroLede: "Systemmag con\u00e7oit des fermetures magn\u00e9tiques int\u00e9gr\u00e9es aux textiles, accessoires et \u00e9quipements techniques, pour cr\u00e9er des syst\u00e8mes discrets, souples et adapt\u00e9s \u00e0 vos contraintes d\u2019usage.",
    heroPrimary: "Cadrer une application",
    heroSecondary: "Comprendre le geste",
    stageStatus: "qualification en cours",
    readoutOneLabel: "Maintien r\u00e9parti",
    readoutOneValue: "sans pi\u00e8ce apparente",
    readoutTwoLabel: "Ouverture naturelle",
    readoutTwoValue: "aimant apr\u00e8s aimant",
    stripOne: "Paris, depuis 2000",
    stripTwo: "Brevets internationaux",
    stripFive: "prototype \u00e0 production",
    principleEyebrow: "Technologie en 4 gestes",
    principleTitle: "Positionner, maintenir, ajuster, ouvrir.",
    principleIntro: "Les polarit\u00e9s altern\u00e9es guident la fermeture. Le maintien se r\u00e9partit dans le textile, puis l\u2019ouverture progresse naturellement par pelage.",
    stepOneTitle: "Positionner",
    stepOneText: "Les modules compatibles se rapprochent et trouvent leur alignement.",
    stepTwoTitle: "Maintenir",
    stepTwoText: "La force se r\u00e9partit dans la longueur pour rester discr\u00e8te au port\u00e9.",
    stepThreeTitle: "Ajuster",
    stepThreeText: "Le module peut coulisser dans son fourreau pour r\u00e9gler le serrage.",
    stepFourTitle: "Ouvrir",
    stepFourText: "Le geste de pelage lib\u00e8re les aimants progressivement.",
    productsEyebrow: "Formats d\u2019int\u00e9gration",
    productsTitle: "Choisir le bon point de d\u00e9part.",
    productZipTitle: "Zips magn\u00e9tiques",
    productZipText: "Fermetures lin\u00e9aires pour v\u00eatements, sacs et \u00e9quipements, avec espacement et puissance ajust\u00e9s au support.",
    productBlockTitle: "Blocs aimant\u00e9s",
    productBlockText: "Modules localis\u00e9s pour cr\u00e9er des zones d\u2019alignement, de maintien ou de r\u00e9glage ponctuel.",
    productSheathTitle: "Fourreaux textiles",
    productSheathText: "Enveloppes pr\u00eates \u00e0 assembler, pens\u00e9es pour prot\u00e9ger le module et contr\u00f4ler le coulissement.",
    productStripTitle: "Bandes d\u2019aimants",
    productStripText: "Base souple pour prototypage rapide et fermeture lin\u00e9aire int\u00e9gr\u00e9e au textile.",
    applicationsEyebrow: "March\u00e9s",
    applicationsTitle: "Une technologie, plusieurs march\u00e9s.",
    appAdaptiveTitle: "Mobilit\u00e9 r\u00e9duite",
    appAdaptiveText: "Rendre le geste plus simple quand la pr\u00e9cision ou la force sont limit\u00e9es.",
    appApparelTitle: "Mode et accessoires",
    appApparelText: "Finition invisible, tomb\u00e9 pr\u00e9serv\u00e9 et manipulation fluide.",
    appMedicalTitle: "M\u00e9dical et orthop\u00e9die",
    appMedicalText: "Orth\u00e8ses, proth\u00e8ses, sangles et v\u00eatements sp\u00e9cialis\u00e9s \u00e0 manipuler simplement.",
    appGearTitle: "Bagagerie et \u00e9quipement",
    appGearText: "Parties amovibles, bretelles, poches, accessoires et syst\u00e8mes modulaires.",
    appDefenseTitle: "D\u00e9fense et outdoor",
    appDefenseText: "Fermeture rapide, r\u00e9sistance en traction et manipulation sous contrainte.",
    appIndustryTitle: "Industrie technique",
    appIndustryText: "Maintien de pi\u00e8ces, protections, housses et fermetures int\u00e9gr\u00e9es \u00e0 des supports \u00e9pais.",
    marketCta: "En savoir plus",
    marketDefenseTitle: "D\u00e9fense",
    marketDefenseText: "Maintien discret, manipulation rapide et r\u00e9duction des pi\u00e8ces apparentes.",
    marketSportTitle: "Sport extr\u00eame",
    marketSportText: "Froid, mouvement, gants ou couches techniques : un geste plus tol\u00e9rant.",
    marketMedicalTitle: "M\u00e9dical",
    marketMedicalText: "Faciliter l\u2019habillage, l\u2019acc\u00e8s et la manipulation sur textiles sp\u00e9cialis\u00e9s.",
    marketFashionTitle: "Accessoires et mode",
    marketFashionText: "Finition invisible, tomb\u00e9 pr\u00e9serv\u00e9 et geste plus fluide.",
    marketIndustryTitle: "Industrie technique",
    marketIndustryText: "Int\u00e9gration fiable sur housses, protections, supports \u00e9pais ou pi\u00e8ces manipul\u00e9es souvent.",
    marketMobilityTitle: "Mobilit\u00e9 r\u00e9duite",
    marketMobilityText: "Rendre le geste plus simple quand la pr\u00e9cision ou la force sont limit\u00e9es.",
    processEyebrow: "Du prototype \u00e0 la s\u00e9rie",
    processTitle: "Une int\u00e9gration valid\u00e9e avant production.",
    processOneTitle: "Cadrage",
    processOneText: "Usage, mati\u00e8re, effort utile et contraintes d\u2019int\u00e9gration.",
    processTwoTitle: "Format",
    processTwoText: "Choix du module, de l\u2019\u00e9cartement, du grade d\u2019aimant et du support.",
    processThreeTitle: "Prototype",
    processThreeText: "Montage fonctionnel sur mati\u00e8re r\u00e9elle pour tester le geste et la tenue.",
    processFourTitle: "Industrialisation",
    processFourText: "Dimensions, nomenclature, pose, contr\u00f4le et accompagnement s\u00e9rie.",
    contactEyebrow: "D\u00e9marrer un projet",
    contactTitle: "Int\u00e9grez SYSTEMMAG dans vos produits.",
    contactText: "D\u00e9crivez votre support, votre usage et la contrainte \u00e0 r\u00e9soudre. Nous vous aidons \u00e0 identifier le format le plus pertinent pour un premier prototype.",
    contactBriefTitle: "Premier prototype",
    contactBriefText: "Support, force, geste, int\u00e9gration.",
    contactAddressLabel: "Atelier Paris",
    contactMap: "Voir sur la carte",
    cueOne: "positionner",
    cueTwo: "maintenir",
    cueThree: "ajuster",
    cueFour: "ouvrir",
    hiwEyebrow: "Comment ça fonctionne",
    hiwTitle: "Comment ça fonctionne ?",
    hiwIntro: "Les deux parties se rapprochent, se positionnent et s’attirent automatiquement grâce aux polarités alternées. L’ouverture se fait ensuite par pelage, aimant après aimant.",
    hiwOneTitle: "Auto-positionnement",
    hiwOneText: "Deux bandes textiles s’approchent : les zones aimantées s’alignent poche à poche.",
    hiwTwoTitle: "Maintien réparti",
    hiwTwoText: "Un rabat se superpose partiellement sur l’autre le long de l’arête magnétique.",
    hiwThreeTitle: "Ouverture par pelage",
    hiwThreeText: "On soulève une extrémité et l’ouverture progresse avec un geste naturel."
  },  en: {
    navTechnology: "Technology",
    navProducts: "Integration",
    navApplications: "Markets",
    navProcess: "Prototype",
    navContact: "Contact",
    navCta: "Start a project",
    heroEyebrow: "Patented technology for textile and equipment",
    heroTitle: "The magnetic closure designed to disappear.",
    heroLede: "Systemmag designs magnetic closures integrated into textiles, accessories, and technical equipment to create discreet, flexible systems adapted to your usage constraints.",
    heroPrimary: "Frame an application",
    heroSecondary: "Understand the gesture",
    stageStatus: "qualification in progress",
    readoutOneLabel: "Distributed hold",
    readoutOneValue: "hidden in textile",
    readoutTwoLabel: "Natural opening",
    readoutTwoValue: "magnet by magnet",
    stripOne: "Paris, since 2000",
    stripTwo: "International patents",
    stripFive: "prototype to production",
    principleEyebrow: "Technology in 4 gestures",
    principleTitle: "Position, hold, adjust, open.",
    principleIntro: "Alternating polarities guide the closure. The hold is distributed through the textile, then opening progresses naturally by peeling.",
    stepOneTitle: "Position",
    stepOneText: "Compatible modules approach each other and find their alignment.",
    stepTwoTitle: "Hold",
    stepTwoText: "Force is distributed along the length while staying discreet in use.",
    stepThreeTitle: "Adjust",
    stepThreeText: "The module can slide inside its sheath for precise tightening.",
    stepFourTitle: "Open",
    stepFourText: "The peel gesture releases magnets progressively.",
    productsEyebrow: "Integration formats",
    productsTitle: "Choose the right starting point.",
    productZipTitle: "Magnetic zips",
    productZipText: "Linear closures for garments, bags, and equipment, with spacing and holding force tuned to the support.",
    productBlockTitle: "Magnet blocks",
    productBlockText: "Localized modules for alignment, holding, or precise adjustment zones.",
    productSheathTitle: "Textile sheaths",
    productSheathText: "Ready-to-assemble envelopes designed to protect the module and control sliding.",
    productStripTitle: "Magnet strips",
    productStripText: "Flexible base for fast prototyping and linear closure inside textile products.",
    applicationsEyebrow: "Markets",
    applicationsTitle: "One technology, several markets.",
    appAdaptiveTitle: "Reduced mobility",
    appAdaptiveText: "Make fastening simpler when precision or strength are limited.",
    appApparelTitle: "Fashion and accessories",
    appApparelText: "Invisible finish, preserved drape, and a more fluid gesture.",
    appMedicalTitle: "Medical and orthopedics",
    appMedicalText: "Orthoses, prostheses, straps, and specialized garments made simple to handle.",
    appGearTitle: "Luggage and equipment",
    appGearText: "Removable parts, straps, pockets, accessories, and modular systems.",
    appDefenseTitle: "Defense and outdoor",
    appDefenseText: "Fast closing, tensile resistance, and handling under constraint.",
    appIndustryTitle: "Technical industry",
    appIndustryText: "Part holding, protections, covers, and closures integrated into thick supports.",
    marketCta: "Learn more",
    marketDefenseTitle: "Defense",
    marketDefenseText: "Discreet hold, fast handling, and fewer visible hardware parts.",
    marketSportTitle: "Extreme sport",
    marketSportText: "Cold, movement, gloves, or technical layers: a more tolerant gesture.",
    marketMedicalTitle: "Medical",
    marketMedicalText: "Make dressing, access, and handling easier on specialized textiles.",
    marketFashionTitle: "Accessories and fashion",
    marketFashionText: "Invisible finish, preserved drape, and a more fluid gesture.",
    marketIndustryTitle: "Technical industry",
    marketIndustryText: "Reliable integration on covers, protections, thick supports, or frequently handled parts.",
    marketMobilityTitle: "Reduced mobility",
    marketMobilityText: "Make fastening simpler when precision or strength are limited.",
    processEyebrow: "Prototype to series",
    processTitle: "Integration validated before production.",
    processOneTitle: "Framing",
    processOneText: "Use, material, useful force, and integration constraints.",
    processTwoTitle: "Format",
    processTwoText: "Selection of module, spacing, magnet grade, and support.",
    processThreeTitle: "Prototype",
    processThreeText: "Functional assembly on real material to test gesture and hold.",
    processFourTitle: "Industrialization",
    processFourText: "Dimensions, bill of materials, installation, quality control, and series support.",
    contactEyebrow: "Start a project",
    contactTitle: "Integrate SYSTEMMAG into your products.",
    contactText: "Describe your support, use case, and constraint. We help identify the most relevant format for a first prototype.",
    contactBriefTitle: "First prototype",
    contactBriefText: "Support, force, gesture, integration.",
    contactAddressLabel: "Paris workshop",
    contactMap: "Open map",
    cueOne: "position",
    cueTwo: "hold",
    cueThree: "adjust",
    cueFour: "open",
    hiwEyebrow: "How it works",
    hiwTitle: "How does it work?",
    hiwIntro: "The two parts come together, position themselves, and attract automatically thanks to alternating polarities. Opening then happens by peeling, magnet after magnet.",
    hiwOneTitle: "Self-positioning",
    hiwOneText: "Two textile strips come together: the magnetized zones align pocket to pocket.",
    hiwTwoTitle: "Distributed hold",
    hiwTwoText: "One flap partially overlaps the other along the magnetic ridge.",
    hiwThreeTitle: "Peel-open",
    hiwThreeText: "You lift one end and the opening progresses with a natural gesture."
  }
};

const html = document.documentElement;
const toggle = document.querySelector("[data-lang-toggle]");
const themeToggle = document.querySelector("[data-theme-toggle]");
const languageOptions = document.querySelectorAll("[data-lang-option]");
const themeOptions = document.querySelectorAll("[data-theme-option]");
const translatable = document.querySelectorAll("[data-i18n]");
const revealItems = document.querySelectorAll("[data-reveal]");
const cursorField = document.querySelector(".cursor-field");
document.body.classList.add("js-enabled");
const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

document.querySelectorAll(".product-media img").forEach((image) => {
  image.addEventListener("error", () => {
    image.closest(".product-media")?.classList.add("is-missing");
    image.remove();
  }, { once: true });
});
document.querySelectorAll(".hiw-media img").forEach((image) => {
  image.addEventListener("error", () => {
    image.closest(".hiw-media")?.classList.add("is-missing");
    image.remove();
  }, { once: true });
});

function setLanguage(lang) {
  html.lang = lang;
  translatable.forEach((node) => {
    const key = node.dataset.i18n;
    if (copy[lang][key]) {
      node.textContent = copy[lang][key];
    }
  });
  languageOptions.forEach((option) => {
    option.classList.toggle("is-active", option.dataset.langOption === lang);
  });
  try {
    localStorage.setItem("systemmag-lang", lang);
  } catch {
    // Some locked-down preview contexts block localStorage.
  }
}

toggle?.addEventListener("click", () => {
  const next = html.lang === "fr" ? "en" : "fr";
  setLanguage(next);
});

function storedValue(key) {
  try {
    return localStorage.getItem(key);
  } catch {
    return null;
  }
}

function setTheme(theme) {
  const resolvedTheme = theme === "light" ? "light" : "dark";
  html.dataset.theme = resolvedTheme;
  themeOptions.forEach((option) => {
    option.classList.toggle("is-active", option.dataset.themeOption === resolvedTheme);
  });
  try {
    localStorage.setItem("systemmag-theme", resolvedTheme);
  } catch {
    // Some locked-down preview contexts block localStorage.
  }
}

themeToggle?.addEventListener("click", () => {
  const next = html.dataset.theme === "light" ? "dark" : "light";
  setTheme(next);
});

const preferredLanguage = storedValue("systemmag-lang");
const storedTheme = storedValue("systemmag-theme");
const prefersLight = window.matchMedia?.("(prefers-color-scheme: light)").matches;
setLanguage(preferredLanguage === "en" ? "en" : "fr");
setTheme(storedTheme || (prefersLight ? "light" : "dark"));

const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add("is-visible");
        observer.unobserve(entry.target);
      }
    });
  },
  { threshold: 0.16, rootMargin: "0px 0px -8% 0px" }
);

// Stagger reveals that share a group container, so children arrive in sequence.
document.querySelectorAll("[data-reveal-group]").forEach((group) => {
  group.querySelectorAll("[data-reveal]").forEach((el, i) => {
    el.style.setProperty("--reveal-i", i);
  });
});

function revealVisibleNow() {
  revealItems.forEach((item) => {
    const rect = item.getBoundingClientRect();
    if (rect.top < window.innerHeight * 0.96 && rect.bottom > 0) {
      item.classList.add("is-visible");
    }
  });
}

window.addEventListener("load", revealVisibleNow);
window.addEventListener("hashchange", () => requestAnimationFrame(revealVisibleNow));
revealVisibleNow();
revealItems.forEach((item) => observer.observe(item));

// ---- Parallax depth layers ----
const parallaxItems = prefersReducedMotion
  ? []
  : Array.from(document.querySelectorAll("[data-parallax]"));
let parallaxRaf = 0;

function updateParallax() {
  parallaxRaf = 0;
  const viewport = window.innerHeight || 1;
  parallaxItems.forEach((el) => {
    const speed = parseFloat(el.dataset.parallax) || 0;
    const rect = el.getBoundingClientRect();
    const centerOffset = rect.top + rect.height / 2 - viewport / 2;
    el.style.setProperty("--px", `${(centerOffset * speed).toFixed(1)}px`);
  });
}

function requestParallax() {
  if (!parallaxRaf) parallaxRaf = requestAnimationFrame(updateParallax);
}

if (parallaxItems.length) {
  window.addEventListener("scroll", requestParallax, { passive: true });
  window.addEventListener("resize", requestParallax);
  updateParallax();
}

window.addEventListener("pointermove", (event) => {
  if (!cursorField) return;
  document.body.style.setProperty("--mx", `${event.clientX}px`);
  document.body.style.setProperty("--my", `${event.clientY}px`);
});

window.addEventListener("pointerleave", () => {
  if (cursorField) cursorField.style.opacity = "0";
});

window.addEventListener("pointerenter", () => {
  if (cursorField) cursorField.style.opacity = "1";
});
const scrollVideo = document.querySelector("[data-scroll-video]");
const cues = document.querySelectorAll(".cue");
let videoReady = false;
let rafId = 0;

function videoRange() {
  const firstSection = document.querySelector(".hero");
  const videoEndSection = document.querySelector("#how-it-works") || document.querySelector(".products");
  const start = firstSection ? firstSection.offsetTop : 0;
  const end = videoEndSection ? videoEndSection.offsetTop - window.innerHeight * 0.08 : window.innerHeight * 2;
  return {
    start,
    end: Math.max(start + 1, end),
  };
}

function regionProgress() {
  const { start, end } = videoRange();
  const progress = (window.scrollY - start) / (end - start);
  return Math.min(1, Math.max(0, progress));
}

function setCue(progress) {
  if (!cues.length) return;
  const index = Math.min(cues.length - 1, Math.floor(progress * cues.length));
  cues.forEach((cue, cueIndex) => {
    cue.classList.toggle("is-active", cueIndex === index);
  });
}

function scrubVideo() {
  rafId = 0;
  const progress = regionProgress();
  const { end } = videoRange();
  const pastVideoRange = window.scrollY >= end;
  document.body.classList.toggle("past-video-range", pastVideoRange);
  setCue(progress);
  if (!scrollVideo || !videoReady || !Number.isFinite(scrollVideo.duration)) return;
  const targetTime = progress * Math.max(0.01, scrollVideo.duration - 0.04);
  if (Math.abs(scrollVideo.currentTime - targetTime) > 0.025) {
    scrollVideo.currentTime = targetTime;
  }
}

function requestScrub() {
  if (!rafId) rafId = requestAnimationFrame(scrubVideo);
}

if (scrollVideo) {
  scrollVideo.addEventListener("loadedmetadata", () => {
    videoReady = scrollVideo.duration > 0;
    document.body.classList.toggle("has-scroll-video", videoReady);
    requestScrub();
  });

  scrollVideo.addEventListener("error", () => {
    videoReady = false;
    document.body.classList.remove("has-scroll-video");
  });

  scrollVideo.load();
  window.addEventListener("scroll", requestScrub, { passive: true });
  window.addEventListener("resize", requestScrub);
  requestScrub();
}




const marketScene = document.querySelector(".applications");
const marketGallery = document.querySelector(".market-gallery");
const marketCards = document.querySelectorAll(".market-card");
let marketRafId = 0;

function updateMarketScene() {
  marketRafId = 0;
  if (!marketScene || !marketGallery || !marketCards.length) return;

  const sceneRect = marketScene.getBoundingClientRect();
  const viewport = window.innerHeight || 1;

  // Skip the per-card style writes entirely when the scene is off-screen.
  if (sceneRect.bottom < -viewport || sceneRect.top > viewport) return;

  const travel = Math.max(1, sceneRect.height - viewport);
  const progress = Math.min(1, Math.max(0, -sceneRect.top / travel));
  const maxIndex = marketCards.length - 1;
  // Hold the first and last cards centred for a beat. A plain
  // progress * maxIndex mapping only ever "leaves" card 0 and only "arrives"
  // at the last card, so the ends get half the dwell of the middle cards.
  // Padding the domain by `hold` index-units at each end and clamping gives
  // every card a symmetric arrive-hold-leave moment.
  const hold = 0.6;
  const floatIndex = Math.min(
    maxIndex,
    Math.max(0, progress * (maxIndex + hold * 2) - hold)
  );
  const activeIndex = Math.round(floatIndex);

  marketScene.style.setProperty("--scene-progress", progress.toFixed(4));
  marketGallery.style.setProperty("--scene-progress", progress.toFixed(4));
  marketGallery.style.setProperty("--scene-index", floatIndex.toFixed(4));

  marketCards.forEach((card, index) => {
    const distance = index - floatIndex;
    const abs = Math.abs(distance);
    const active = Math.max(0, 1 - abs);
    const near = Math.max(0, 1 - abs / 1.6);
    const direction = Math.sign(distance || 0);
    card.style.setProperty("--card-distance", distance.toFixed(4));
    card.style.setProperty("--card-abs", abs.toFixed(4));
    card.style.setProperty("--card-active", active.toFixed(4));
    card.style.setProperty("--card-near", near.toFixed(4));
    card.style.setProperty("--card-x", (distance * 118).toFixed(2) + "%");
    card.style.setProperty("--card-y", (Math.min(abs, 1.8) * 18).toFixed(2) + "%");
    card.style.setProperty("--card-tilt", (direction * Math.min(abs, 1) * -9).toFixed(2) + "deg");
    card.style.setProperty("--card-glow-x", (50 + distance * -28).toFixed(2) + "%");
    card.style.setProperty("--liquid-progress", active.toFixed(4));
    card.style.setProperty("--liquid-center", near.toFixed(4));
    card.classList.toggle("is-scene-active", index === activeIndex);
    card.classList.toggle("is-liquid-active", near > 0.28);
  });
}

function requestMarketScene() {
  if (!marketRafId) marketRafId = requestAnimationFrame(updateMarketScene);
}

if (marketCards.length) {
  document.body.classList.add("js-markets-ready");
  window.addEventListener("scroll", requestMarketScene, { passive: true });
  window.addEventListener("resize", requestMarketScene);
  window.addEventListener("load", requestMarketScene);
  requestMarketScene();
}


// ---- Smooth scroll (Lenis) ----
// Lenis interpolates scroll position every frame, which lets the scrubbed video,
// the markets carousel, parallax, and reveals all glide instead of stepping.
let lenis = null;

if (!prefersReducedMotion && typeof Lenis !== "undefined") {
  lenis = new Lenis({
    lerp: 0.085,
    wheelMultiplier: 1,
    smoothWheel: true,
  });

  const raf = (time) => {
    lenis.raf(time);
    requestAnimationFrame(raf);
  };
  requestAnimationFrame(raf);

  // Lenis programmatically updates window scroll, so the existing window "scroll"
  // listeners (video scrub, markets) still fire; this just keeps parallax in lockstep.
  lenis.on("scroll", requestParallax);
}

// Route in-page anchors through Lenis for an eased glide to the target.
document.querySelectorAll('a[href^="#"]').forEach((link) => {
  link.addEventListener("click", (event) => {
    const href = link.getAttribute("href");
    if (!href || href.length < 2) return;
    const target = document.querySelector(href);
    if (!target) return;
    event.preventDefault();
    if (lenis) {
      lenis.scrollTo(target, { offset: -88, duration: 1.15 });
    } else {
      target.scrollIntoView({ behavior: "smooth" });
    }
  });
});








// ---- Header section state ----
const navLinks = Array.from(document.querySelectorAll('.site-nav a[href^="#"]'));
const navTargets = navLinks
  .map((link) => ({ link, target: document.querySelector(link.getAttribute('href')) }))
  .filter((item) => item.target);
let navRafId = 0;

function updateNavState() {
  navRafId = 0;
  if (!navTargets.length) return;

  const marker = window.innerHeight * 0.38;
  let current = navTargets[0];

  navTargets.forEach((item) => {
    const rect = item.target.getBoundingClientRect();
    if (rect.top <= marker && rect.bottom > 120) current = item;
  });

  navTargets.forEach(({ link }) => {
    const isCurrent = link === current.link;
    link.classList.toggle('is-current', isCurrent);
    if (isCurrent) {
      link.setAttribute('aria-current', 'true');
    } else {
      link.removeAttribute('aria-current');
    }
  });
}

function requestNavState() {
  if (!navRafId) navRafId = requestAnimationFrame(updateNavState);
}

if (navTargets.length) {
  window.addEventListener('scroll', requestNavState, { passive: true });
  window.addEventListener('resize', requestNavState);
  window.addEventListener('load', requestNavState);
  updateNavState();
}



